meta {
  name: Move Item
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/playlists/{{playlistId}}/move
  body: json
  auth: none
}

body:json {
  {
    "mediaId": "{{mediaId}}",
    "newPosition": 0
  }
}
